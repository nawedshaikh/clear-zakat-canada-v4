import{describe,it,expect}from'vitest';import{initial,calculate}from'./engine';describe('engine',()=>{it('uses full equity by default',()=>{const d=initial();d.selected.nonreg=true;d.nonreg.equity=1000;expect(calculate(d).nonreg).toBe(1000)});it('supports proxy',()=>{const d=initial();d.selected.nonreg=true;d.nonreg.equity=1000;d.nonreg.equityMethod='proxy';expect(calculate(d).nonreg).toBe(300)});it('prices crypto',()=>{const d=initial();d.selected.nonreg=true;d.prices.btc=100000;d.nonreg.btcQty=.1;expect(calculate(d).crypto).toBe(10000)});it('below nisab is exempt',()=>{const d=initial();d.prices.silver=1;d.bank.cash=500;expect(calculate(d).due).toBe(false)})});
